<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="120dp"
    android:layout_height="180dp"
    android:layout_margin="6dp"
    app:cardCornerRadius="8dp"
    app:cardBackgroundColor="#202020"
    android:clickable="true"
    android:focusable="true">

    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/imgPoster"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="centerCrop"
            android:src="@mipmap/ic_launcher"/>

        <View
            android:layout_width="match_parent"
            android:layout_height="100dp"
            android:layout_gravity="bottom"
            android:background="@drawable/gradient_black_bottom"/>

        <ImageView
            android:id="@+id/imgLogo"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:layout_gravity="bottom|center_horizontal"
            android:layout_marginBottom="10dp"
            android:layout_marginHorizontal="4dp"
            android:scaleType="fitCenter"
            android:visibility="invisible" />

        <ImageView
            android:id="@+id/imgDownload"
            android:layout_width="28dp"
            android:layout_height="28dp"
            android:layout_gravity="top|end" 
            android:layout_margin="8dp"
            android:background="@drawable/circle_download_bg"
            android:padding="5dp"
            android:src="@drawable/ic_download"
            app:tint="@android:color/white"
            android:scaleType="centerInside"/>

        <TextView
            android:id="@+id/tvName"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom"
            android:padding="4dp"
            android:text="Carregando..."
            android:textColor="@color/white"
            android:textSize="11sp"
            android:maxLines="2"
            android:ellipsize="end"
            android:gravity="center"
            android:visibility="gone"/>

        <!-- Borda de foco — VodActivity.kt já tem lógica pronta pra
             mostrar/esconder isso via isTelevision(), só faltava a view -->
        <View
            android:id="@+id/viewFocusBorder"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:background="@drawable/bg_card_focus_border"
            android:visibility="invisible"/>

    </FrameLayout>

</androidx.cardview.widget.CardView>
